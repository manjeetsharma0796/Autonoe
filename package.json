{
  "name": "autonoe",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Autonoe — autonomous AI trading thesis & debate copilot on Mantle Sepolia",
  "workspaces": [
    "packages/*",
    "server"
  ],
  "scripts": {
    "build": "tsc -b",
    "typecheck": "bun --filter '*' typecheck",
    "test": "bun --filter '*' test",
    "lint:todo": "bun scripts/lint-todo.ts"
  },
  "devDependencies": {
    "@types/bun": "^1.3.14",
    "@types/node": "^22.9.0",
    "typescript": "^5.6.3"
  },
  "engines": {
    "node": ">=20"
  }
}
